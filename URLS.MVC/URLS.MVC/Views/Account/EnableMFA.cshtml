@model URLS.Models.MFAViewModel

@{
    ViewData["Title"] = "EnableMFA";
}

<h1>EnableMFA</h1>

<h4>ErrorViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EnableMFA">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ManualEntryKey" class="control-label">@Model.ManualEntryKey</label>
                <input asp-for="ManualEntryKey" class="form-control"/>
            </div>
            <div class="form-group">
                <img src="@Model.QrCodeImage"/>
            </div>
            <div class="form-group">
                <input type="submit" value="Прив'язати" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

